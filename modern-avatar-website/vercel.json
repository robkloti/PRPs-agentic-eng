{
  "version": 2,
  "buildCommand": "cp -r public/* . && echo 'Files copied for static deployment'",
  "outputDirectory": ".",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}