# Billing - @tm/billing

This package is responsible for managing billing and subscription related operations.

Make sure the app installs the `@tm/billing` package before using it.

```json
{
  "name": "my-app",
  "dependencies": {
    "@tm/billing": "*"
  }
}
```
